# Spec: api/refactor/anti-patterns

This change is scoped to authentication/authorization clean-up for the vouchers module and controller-level clean-up for the sessions module. See `specs/auth/spec.md` for the full requirements.
