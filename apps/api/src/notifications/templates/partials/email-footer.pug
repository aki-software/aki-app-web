table(role="presentation" width="100%" cellpadding="0" cellspacing="0")
  tr
    td(align="center")
      p(style=`margin:0 0 8px 0;font-size:11px;color:${colors.text.secondary};letter-spacing:0.08em;text-transform:uppercase;font-weight:700;`) A.ki - Orientación vocacional asistida
      p(style=`margin:0 0 14px 0;font-size:12px;color:${colors.outline.light};`) #{brandDomain} · #{supportEmail}
      p(style=`margin:0;font-size:11px;color:${colors.outline.light};opacity:0.7;`) Documento reservado para fines orientativos.
